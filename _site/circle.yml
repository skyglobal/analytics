test:
  override:
    - rake functional

deployment:
  production:
    branch: master
    commands:
      - rake deploy_new_version
      - git checkout gh-pages
      - git push origin master:gh-pages
